#include "tx.h"

#include "../p2pkh/p2pkh.h"


namespace tx
{


/*
template< typename T>
void Tx<T>::set( T *target )
{
	_ptr = target;
}



template <typename T>
T* Tx<T>::get()
{

	return static_cast<P2PKH>( _ptr );

	// 今のところは1種類しかない為,一旦これで
	// 将来的にはトランザクションの中身に応じて適切な型に変換して返却する

	return _ptr;
}
*/






};
